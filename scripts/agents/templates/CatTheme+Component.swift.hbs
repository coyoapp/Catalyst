//
//  CatTheme+{{PascalName}}.swift
//  Catalyst
//
//  Created by Catalyst Agent on {{date}}.
//
//  !! GENERATED BY SCAFFOLD AGENT — REVIEW BEFORE MOVING TO SOURCE !!
//  Destination: iOS/Catalyst/Sources/Catalyst/Theme/
//
//  Extends CatTheme with a {{PascalName}} config resolver.
//  Keeping component configs in separate extension files prevents CatTheme.swift
//  from growing into a monolith. Pattern established by CatTheme+Button.swift.
//

import SwiftUI

// MARK: - {{PascalName}}Config

public extension CatTheme {

    /// Resolves a `Cat{{PascalName}}StateStyleConfig` for the given variant and color combination.
    ///
    /// This is the single source of truth that maps `(ButtonVariant × ColorConfig)` onto the
    /// design-token colors in `CatColors.Theme.*`. Use `.cat{{PascalName}}Config(variant:color:)` on a
    /// view to inject this into the SwiftUI environment instead of passing `styleConfig:` directly.
    ///
    /// - Parameters:
    ///   - variant: The visual shape of the component (`.filled`, `.outlined`, `.text`, `.link`).
    ///   - color: The semantic color role (`.primary`, `.danger`, `.success`, etc.).
    ///   - theme: The active theme. Defaults to `CatTheme.current`.
    ///   - accentPalette: An optional pre-resolved `CatColorPalette` injected via
    ///     `.catalystAccentColor(_:)`. When non-nil and `color == .primary`, this palette is used
    ///     instead of the registry entry, enabling per-subtree whitelabeling.
    /// - Returns: A fully-resolved `Cat{{PascalName}}StateStyleConfig` for all interaction states.
    static func {{camelName}}Config(
        variant: ButtonVariant = .filled,
        color: ColorConfig = .primary,
        theme: ThemeType = CatTheme.current,
        accentPalette: CatColorPalette? = nil
    ) -> Cat{{PascalName}}StateStyleConfig {

        let colorPalette: CatColorPalette
        if color == .primary, let injected = accentPalette {
            colorPalette = injected
        } else {
            colorPalette = color.palette(for: theme)
        }

        // Shared disabled state — always muted regardless of variant/color.
        let disabledState = Cat{{PascalName}}StateStyle(
            colorStyle: Cat{{PascalName}}StateColorStyle(
                background: Color.clear,
                foreground: CatColors.Ui.Font.muted,
                border: Color.clear
            ),
            properties: Cat{{PascalName}}StateProperties(isUnderlined: false, hasSecondaryFocusRing: false, scale: 1)
        )

        let commonProperties = Cat{{PascalName}}StateProperties(
            isUnderlined: false,
            hasSecondaryFocusRing: false,
            scale: 1
        )

        switch variant {

        // MARK: Filled — solid background, fill foreground
        case .filled:
            return Cat{{PascalName}}StateStyleConfig(
                normal: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: colorPalette.bg,
                        foreground: colorPalette.fill,
                        border: Color.clear
                    ),
                    properties: commonProperties
                ),
                hovered: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: colorPalette.bgHover,
                        foreground: colorPalette.fillHover,
                        border: Color.clear
                    ),
                    properties: commonProperties
                ),
                pressed: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: colorPalette.bgActive,
                        foreground: colorPalette.fillActive,
                        border: Color.clear
                    ),
                    properties: commonProperties
                ),
                focused: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: colorPalette.bg,
                        foreground: colorPalette.fill,
                        border: Color.clear
                    ),
                    properties: Cat{{PascalName}}StateProperties(isUnderlined: false, hasSecondaryFocusRing: true, scale: 1)
                ),
                disabled: Cat{{PascalName}}StateStyle(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: CatColors.Ui.Background.muted,
                        foreground: CatColors.Ui.Font.muted,
                        border: Color.clear
                    ),
                    properties: commonProperties
                )
            )

        // MARK: Outlined — transparent background, colored border and text
        case .outlined:
            return Cat{{PascalName}}StateStyleConfig(
                normal: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: Color.clear,
                        foreground: colorPalette.text,
                        border: colorPalette.bg.opacity(0.20)
                    ),
                    properties: commonProperties
                ),
                hovered: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: colorPalette.bgHover.opacity(0.05),
                        foreground: colorPalette.text,
                        border: colorPalette.bg.opacity(0.20)
                    ),
                    properties: commonProperties
                ),
                pressed: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: colorPalette.bgActive.opacity(0.15),
                        foreground: colorPalette.text,
                        border: colorPalette.bg.opacity(0.20)
                    ),
                    properties: commonProperties
                ),
                focused: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: Color.clear,
                        foreground: colorPalette.text,
                        border: colorPalette.bg.opacity(0.20)
                    ),
                    properties: Cat{{PascalName}}StateProperties(isUnderlined: false, hasSecondaryFocusRing: true, scale: 1)
                ),
                disabled: Cat{{PascalName}}StateStyle(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: Color.clear,
                        foreground: CatColors.Ui.Font.muted,
                        border: CatColors.Ui.Font.muted.opacity(0.20)
                    ),
                    properties: commonProperties
                )
            )

        // MARK: Text (ghost) — transparent background, no border, colored text
        case .text:
            return Cat{{PascalName}}StateStyleConfig(
                normal: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: Color.clear,
                        foreground: colorPalette.text,
                        border: Color.clear
                    ),
                    properties: commonProperties
                ),
                hovered: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: colorPalette.bgHover.opacity(0.05),
                        foreground: colorPalette.textHover,
                        border: Color.clear
                    ),
                    properties: commonProperties
                ),
                pressed: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: colorPalette.bgActive.opacity(0.15),
                        foreground: colorPalette.textActive,
                        border: Color.clear
                    ),
                    properties: commonProperties
                ),
                focused: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: Color.clear,
                        foreground: colorPalette.text,
                        border: Color.clear
                    ),
                    properties: Cat{{PascalName}}StateProperties(isUnderlined: false, hasSecondaryFocusRing: true, scale: 1)
                ),
                disabled: disabledState
            )

        // MARK: Link — transparent, no border, underline on hover/press
        case .link:
            return Cat{{PascalName}}StateStyleConfig(
                normal: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: Color.clear,
                        foreground: colorPalette.text,
                        border: Color.clear
                    ),
                    properties: commonProperties
                ),
                hovered: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: Color.clear,
                        foreground: colorPalette.textHover,
                        border: Color.clear
                    ),
                    properties: Cat{{PascalName}}StateProperties(isUnderlined: true, hasSecondaryFocusRing: false, scale: 1)
                ),
                pressed: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: Color.clear,
                        foreground: colorPalette.textActive,
                        border: Color.clear
                    ),
                    properties: Cat{{PascalName}}StateProperties(isUnderlined: true, hasSecondaryFocusRing: false, scale: 1)
                ),
                focused: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: Color.clear,
                        foreground: colorPalette.text,
                        border: Color.clear
                    ),
                    properties: Cat{{PascalName}}StateProperties(isUnderlined: false, hasSecondaryFocusRing: true, scale: 1)
                ),
                disabled: disabledState
            )
        }
    }
}
