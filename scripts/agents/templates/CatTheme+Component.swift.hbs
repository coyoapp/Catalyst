//
//  CatTheme+{{PascalName}}.swift
//  Catalyst
//
//  Created by Catalyst Agent on {{date}}.
//
//  !! GENERATED BY SCAFFOLD AGENT — REVIEW BEFORE MOVING TO SOURCE !!
//  Destination: iOS/Catalyst/Sources/Catalyst/Theme/
//
//  Extends CatTheme.Components with {{PascalName}} configs.
//  Keeping component configs in separate extension files prevents CatTheme.swift
//  from growing into a monolith. Pattern established by CatTheme.swift.
//

import SwiftUI

public extension CatTheme.Components {

    enum {{PascalName}}s {

        public enum Primary {

            /// Default filled style — uses primary brand background with fill foreground.
            public static let defaultConfig = Cat{{PascalName}}StateStyleConfig(
                normal: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: CatColors.Theme.Primary.bg,
                        foreground: CatColors.Theme.Primary.fill,
                        border: Color.clear
                    )
                ),
                hovered: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: CatColors.Theme.Primary.bgHover,
                        foreground: CatColors.Theme.Primary.fillHover,
                        border: Color.clear
                    )
                ),
                pressed: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: CatColors.Theme.Primary.bgActive,
                        foreground: CatColors.Theme.Primary.fillActive,
                        border: Color.clear
                    )
                ),
                focused: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: CatColors.Theme.Primary.bg,
                        foreground: CatColors.Theme.Primary.fill,
                        border: Color.clear
                    )
                ),
                disabled: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: CatColors.Ui.Background.muted,
                        foreground: CatColors.Ui.Font.muted,
                        border: Color.clear
                    )
                )
            )

            {{#if hasBorderVariant}}
            /// Border / outline style — transparent background with primary brand border.
            public static let borderConfig = Cat{{PascalName}}StateStyleConfig(
                normal: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: Color.clear,
                        foreground: CatColors.Theme.Primary.text,
                        border: CatColors.Theme.Primary.bg
                    )
                ),
                hovered: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: CatColors.Theme.Primary.bgHover,
                        foreground: CatColors.Theme.Primary.text,
                        border: CatColors.Theme.Primary.bg
                    )
                ),
                pressed: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: CatColors.Theme.Primary.bgActive,
                        foreground: CatColors.Theme.Primary.text,
                        border: CatColors.Theme.Primary.bg
                    )
                ),
                focused: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: Color.clear,
                        foreground: CatColors.Theme.Primary.text,
                        border: CatColors.Theme.Primary.bg
                    )
                ),
                disabled: .init(
                    colorStyle: Cat{{PascalName}}StateColorStyle(
                        background: Color.clear,
                        foreground: CatColors.Ui.Font.muted,
                        border: CatColors.Ui.Font.muted
                    )
                )
            )
            {{/if}}

            // TODO: add additional variant configs (ghost, link, etc.) as needed per spec
        }
    }
}
