// {{PascalName}}.kt
// Catalyst
//
// Created by Catalyst Agent on {{date}}.
//
// !! GENERATED BY SCAFFOLD AGENT — REVIEW BEFORE MOVING TO SOURCE !!
// Destination: android/catalyst/src/main/java/com/haiilo/catalyst/components/
//
// Android skeleton for {{PascalName}}. The iOS implementation is the source of truth
// for interaction states and token wiring. Mirror it here as the Android
// architecture matures.

package com.haiilo.catalyst.components

import androidx.compose.foundation.layout.PaddingValues
import androidx.compose.foundation.layout.padding
import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.material3.Surface
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.tooling.preview.Preview
import androidx.compose.ui.unit.dp
import com.haiilo.catalyst.tokens.generated.CatBorderRadius
import com.haiilo.catalyst.tokens.generated.CatColors
import com.haiilo.catalyst.tokens.generated.CatSpacing
import com.haiilo.catalyst.tokens.generated.CatTypography

// ---------------------------------------------------------------------------
// Data model
// ---------------------------------------------------------------------------

/**
 * Visual state model for [Cat{{PascalName}}].
 * Mirrors Cat{{PascalName}}StateStyleConfig on iOS.
 */
data class Cat{{PascalName}}Colors(
    val background: Color,
    val foreground: Color,
    val border: Color,
)

// TODO: define size enum once design spec is confirmed
enum class Cat{{PascalName}}Size {
    {{#each sizes}}
    {{toUpperCase this.caseName}},
    {{/each}}
    CUSTOM;
}

// ---------------------------------------------------------------------------
// Default configs — wired to Catalyst tokens
// ---------------------------------------------------------------------------

object Cat{{PascalName}}Defaults {

    /** Default (primary filled) visual state — normal. */
    val primaryColors = Cat{{PascalName}}Colors(
        background = CatColors.Theme.Primary.bg,
        foreground = CatColors.Theme.Primary.fill,
        border = Color.Transparent,
    )

    /** Disabled visual state. */
    val disabledColors = Cat{{PascalName}}Colors(
        background = CatColors.Ui.Background.muted,
        foreground = CatColors.Ui.Font.muted,
        border = Color.Transparent,
    )

    // TODO: add hovered / pressed / focused states when Compose interaction
    // source parity with iOS is established.
}

// ---------------------------------------------------------------------------
// Composable
// ---------------------------------------------------------------------------

/**
 * Catalyst {{PascalName}} component.
 *
 * Usage:
 * ```kotlin
 * Cat{{PascalName}}(label = "Draft")
 * Cat{{PascalName}}(label = "Active", enabled = false)
 * ```
 *
 * TODO: expand API surface to match iOS Cat{{PascalName}} (variants, sizes, content model).
 */
@Composable
fun Cat{{PascalName}}(
    // TODO: replace with Cat{{PascalName}}Content model once content model is agreed
    label: String,
    modifier: Modifier = Modifier,
    enabled: Boolean = true,
    colors: Cat{{PascalName}}Colors = if (enabled) Cat{{PascalName}}Defaults.primaryColors
                                       else Cat{{PascalName}}Defaults.disabledColors,
    typography: TextStyle = CatTypography.s1,
    size: Cat{{PascalName}}Size = Cat{{PascalName}}Size.{{toUpperCase defaultSize}},
) {
    Surface(
        modifier = modifier,
        color = colors.background,
        contentColor = colors.foreground,
        shape = RoundedCornerShape(CatBorderRadius.borderRadiusMd),
        // TODO: wire border color (colors.border) once Compose border API is aligned
    ) {
        Text(
            text = label,
            style = typography,
            modifier = Modifier.padding(
                horizontal = CatSpacing.spacingXl,
                vertical = CatSpacing.spacingLg,
            ),
        )
    }
}

// ---------------------------------------------------------------------------
// Preview
// ---------------------------------------------------------------------------

@Preview(showBackground = true)
@Composable
private fun Cat{{PascalName}}Preview() {
    Cat{{PascalName}}(label = "{{PascalName}}")
}

@Preview(showBackground = true)
@Composable
private fun Cat{{PascalName}}DisabledPreview() {
    Cat{{PascalName}}(label = "{{PascalName}} Disabled", enabled = false)
}
